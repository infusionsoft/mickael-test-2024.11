/**
 * Keap REST API
 * Keap REST Core Service
 *
 * The version of the OpenAPI document: 2.70.0.778988
 * Contact: api@keap.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdateStageRequest model module.
 * @module keap.core.v2/model/UpdateStageRequest
 */
class UpdateStageRequest {
    /**
     * Constructs a new <code>UpdateStageRequest</code>.
     * Request for updating a stage.
     * @alias module:keap.core.v2/model/UpdateStageRequest
     * @param name {String} The name of the stage.
     * @param previousStageId {String} The ID of the previous stage.
     * @param nextStageId {String} The ID of the next stage.
     * @param pipelineId {String} The ID of the pipeline.
     */
    constructor(name, previousStageId, nextStageId, pipelineId) { 
        
        UpdateStageRequest.initialize(this, name, previousStageId, nextStageId, pipelineId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, previousStageId, nextStageId, pipelineId) { 
        obj['name'] = name;
        obj['previous_stage_id'] = previousStageId;
        obj['next_stage_id'] = nextStageId;
        obj['pipeline_id'] = pipelineId;
    }

    /**
     * Constructs a <code>UpdateStageRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/UpdateStageRequest} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/UpdateStageRequest} The populated <code>UpdateStageRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateStageRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('previous_stage_id')) {
                obj['previous_stage_id'] = ApiClient.convertToType(data['previous_stage_id'], 'String');
            }
            if (data.hasOwnProperty('next_stage_id')) {
                obj['next_stage_id'] = ApiClient.convertToType(data['next_stage_id'], 'String');
            }
            if (data.hasOwnProperty('pipeline_id')) {
                obj['pipeline_id'] = ApiClient.convertToType(data['pipeline_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateStageRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateStageRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateStageRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['previous_stage_id'] && !(typeof data['previous_stage_id'] === 'string' || data['previous_stage_id'] instanceof String)) {
            throw new Error("Expected the field `previous_stage_id` to be a primitive type in the JSON string but got " + data['previous_stage_id']);
        }
        // ensure the json data is a string
        if (data['next_stage_id'] && !(typeof data['next_stage_id'] === 'string' || data['next_stage_id'] instanceof String)) {
            throw new Error("Expected the field `next_stage_id` to be a primitive type in the JSON string but got " + data['next_stage_id']);
        }
        // ensure the json data is a string
        if (data['pipeline_id'] && !(typeof data['pipeline_id'] === 'string' || data['pipeline_id'] instanceof String)) {
            throw new Error("Expected the field `pipeline_id` to be a primitive type in the JSON string but got " + data['pipeline_id']);
        }

        return true;
    }


}

UpdateStageRequest.RequiredProperties = ["name", "previous_stage_id", "next_stage_id", "pipeline_id"];

/**
 * The name of the stage.
 * @member {String} name
 */
UpdateStageRequest.prototype['name'] = undefined;

/**
 * The ID of the previous stage.
 * @member {String} previous_stage_id
 */
UpdateStageRequest.prototype['previous_stage_id'] = undefined;

/**
 * The ID of the next stage.
 * @member {String} next_stage_id
 */
UpdateStageRequest.prototype['next_stage_id'] = undefined;

/**
 * The ID of the pipeline.
 * @member {String} pipeline_id
 */
UpdateStageRequest.prototype['pipeline_id'] = undefined;






export default UpdateStageRequest;
